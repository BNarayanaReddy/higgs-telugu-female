All scripts compile; nothing run here. Run order (edit config.yaml first):
python prepare_data.py      # 100 held out
python train.py             # LoRA fine-tune → checkpoints/, samples/step_* (LISTEN)
python upload_hf.py         # push WORK_DIR/final_model to the Hub
